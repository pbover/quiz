<table>
<tr>
<td>
<br/>
<form method="get" action="/quizes">
<p>
<span>Buscar pregunta: <span/><input type="text" name="search" id="search" value="<%= search %>" /> <input type="submit" value="Buscar"></p>


<p><br/> </p>
</form>
</td>
</tr>
<% var i; for (i=0;i<quizes.length;i++) {

var temaStr="";
if(quizes[i].tema==='otro')
{
temaStr = 'Otro';
}
else if(quizes[i].tema==='humanidades')
{
temaStr = 'Humanidades';
}
else if(quizes[i].tema==='ocio')
{
temaStr = 'Ocio';
}
else if(quizes[i].tema==='ciencia')
{
temaStr = 'Ciencia';
}
else if(quizes[i].tema==='tecnologia')
{
temaStr = 'Tecnología';
}
else{
temaStr =quizes[i].tema;
}
%>
<tr><td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %> ( <%= temaStr %> )</a></td>
<%if(session.user){%>
<td>
  <a href="quizes/<%= quizes[i].id %>/edit"><button>Editar</button></a>
</td>
<td>
  <form method="post" action="/quizes/<%= quizes[i].id %>?_method=delete">
            <button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">
              borrar
            </button>
  </form>
</td>
<%}%>
</tr>
<%}%>
<%if(session.user){%>
<tr>
<td><a href="/quizes/new"><button>Crear nueva pregunta</button></a></td>
</tr>
<%}%>

</table>
